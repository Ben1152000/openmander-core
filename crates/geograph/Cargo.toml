[package]
name = "geograph"
version = { workspace = true }
edition = { workspace = true }
description = "Self-contained DCEL (Doubly Connected Edge List) for planar graph geometry"

[dependencies]
ahash = "0.8"
geo = "0.30"
rstar = "0.12"
