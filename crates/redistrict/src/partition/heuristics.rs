use crate::partition::WeightedGraphPartition;

impl WeightedGraphPartition {
    /// Equalize all districts by given series, within a given tolerance.
    pub fn equalize(&mut self, series: &str, tolerance: u32) { todo!() }
}
